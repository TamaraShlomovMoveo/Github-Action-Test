import { JWT } from 'google-auth-library';
import { Edit, IEditParams } from './../Edit';
export interface IPromoteParams {
    /** Specify track for this release. Can be one of the [[tracks]]. */
    track: string;
}
export declare class Promote extends Edit {
    promoteParams: IPromoteParams;
    private previousTrack?;
    private previousRelease?;
    constructor(client: JWT, editParams: IEditParams, promoteParams: IPromoteParams);
    makeEdits(): Promise<void>;
    private findRelease;
    private findPreviousTrack;
    private findPreviousRelease;
    private assignTrack;
    private removePreviousTrack;
}
