export interface IPackageManifest {
    /** ID of the package */
    packageName: string;
    /** Version code of the package */
    versionCode: number;
}
/**
 * Parse the manifest of an APK file
 * @param apk Path to the APK file
 */
export declare const parseManifest: (apk: string | string[]) => Promise<IPackageManifest>;
/**
 * Available tracks
 */
export declare const tracks: string[];
/**
 * Check if a track is valid
 * @param track Name of the track to check
 *
 * @returns Does the track exits?
 */
export declare const checkTrack: (track: string) => boolean;
